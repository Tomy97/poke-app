import { Card, CardContent } from "@mui/material";

export const LoginView = () => {
  return (
    <Card>
      <CardContent>
        <h4>
          Login
        </h4>
      </CardContent>
    </Card>
  );
};

export default LoginView;
